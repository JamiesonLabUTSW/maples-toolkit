interface:
  display_name: "OSCE Rubric Transform"
  short_description: "Adapt, score, fill, or restyle rubrics"
  default_prompt: "Use $osce-rubric-transform to adapt this OSCE rubric, add score levels, fill missing fields, or apply a reference style."
