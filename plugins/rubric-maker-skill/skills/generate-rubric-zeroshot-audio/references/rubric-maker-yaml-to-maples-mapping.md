# Rubric Maker YAML → MAPLES Upload Mapping

> **Not the canonical MAPLES platform Excel workbook specification.**
> 
> This document describes the **toolkit row-oriented YAML** accepted by the MCP
> `upload_rubric` tool when `filename` ends in `.yaml` or `.yml`. MAPLES converts this
> YAML into its internal workbook format (`ItemKey`, `Mode`, `Section`, `QuestionText`,
> `Response1..N`) before validation and storage.
> 
> For the platform workbook contract (required station columns, valid modes
> `Audio`/`Video`/`Note`/`Transcript`, scoring columns `Response1..N`), use the
> canonical training specification `maples-rubric-specification-v1.0-mar-2026` (March
> 2026\) in the MAPLES ops training/rubric-authoring bundle — not this file.
> 
> The bundled sample workbook `BloodPressure_OSCE.fixed.xlsx` uses **platform workbook
> columns** as a case-source example.
> Do **not** copy those Excel column names into the YAML upload body; generate the
> toolkit YAML shape defined below.

**Toolkit YAML mapping** Version 1.0 - June 2026

## Overview

The MCP `upload_rubric` tool accepts rubric YAML text through its `content` argument
when `filename` ends in `.yaml` or `.yml`. MAPLES converts this row-oriented YAML into
its internal workbook format before validation and storage.

Use this format when generating a rubric for direct upload:

```yaml
name: example-rubric
description: Example audio rubric
station: Default
version: "1"
context: ""
rubric:
  - ItemKey: NOTE-001
    Category: History
    QuestionName: Documents the chief concern and relevant history.
    ScoringLogic:
      Score1: Missing or unsupported.
      Score2: Partially demonstrated.
      Score3: Clearly and accurately demonstrated.
    Mode: audio
    Technique: Listen for explicit spoken, paralinguistic, or interactional evidence.
    Purpose: Assess completeness and accuracy.
    AdditionalContext: Accept equivalent clinical wording.
```

## Top-Level Fields

| Field | Required | Description |
| --- | --- | --- |
| `rubric` | Yes | Non-empty list of rubric item mappings. |
| `name` | No | Rubric name. Defaults to the uploaded filename without its extension. |
| `description` | No | Rubric description. Defaults to `name`. |
| `station` | No | Station sheet name. Defaults to `Default`; if provided, it must not be blank. |
| `version` | No | Rubric version metadata. Defaults to `"1"`. |
| `context` | No | Optional rubric-wide context. Defaults to an empty string. |

## Rubric Item Fields

| Field | Required | Description |
| --- | --- | --- |
| `ScoringLogic` | Yes | Non-empty mapping of sequential scoring levels named `Score1`, `Score2`, and so on. |
| `ItemKey` | No | Unique item identifier. Defaults to `ITEM-001`, `ITEM-002`, and so on. |
| `Mode` | No | Toolkit modality: `audio`, `video`, or `note` (lowercase). Defaults to `note`. MAPLES normalizes to title case (`Audio`/`Video`/`Note`). Do not invent non-platform modes. |
| `Category` | Yes | Logical grouping for the item. Converted to the internal `Section` field. Required for successful platform validation after conversion (blank `Section` groups poorly; blank `QuestionText` fails). |
| `QuestionName` | Yes | Assessment question or skill description. Converted to `QuestionText`. Required — blank values fail platform validation with `BLANK_QUESTION_TEXT`. |
| `Technique` | No | Exact evidence or technique the grader should look for. |
| `Purpose` | No | Educational or technical rationale for the item. |
| `AdditionalContext` | No | Clarifications, acceptable alternatives, constraints, or grading cautions. |

For rubrics generated by this skill, use `Mode: audio` for every item and include
`Category`, `QuestionName`, `Technique`, `Purpose`, and `AdditionalContext`.

## Scoring Logic

`ScoringLogic` must contain one or more levels ordered from lowest to highest.
Keys must be sequential and must start at `Score1`.

Valid:

```yaml
ScoringLogic:
  Score1: Missing.
  Score2: Partially documented.
  Score3: Clearly documented.
```

Invalid:

```yaml
ScoringLogic:
  Score1: Missing.
  Score3: Clearly documented.
```

The invalid example fails because `Score2` is missing.
The key order also matters: `Score1`, `Score2`, and `Score3` must appear in that order.

## Validation Rules

Uploads fail when:

- the YAML root is not a mapping;
- `rubric` is missing, is not a list, or is empty;
- a rubric item is not a mapping;
- `ScoringLogic` is missing, is not a mapping, or is empty;
- `ScoringLogic` keys are not sequential `Score1` through `ScoreN`;
- `station` is present but blank;
- the rubric name cannot be derived from `name` or the filename.

Use unique `ItemKey` values.
Duplicate keys fail downstream workbook validation.

## Quality Rules

- Grade only what can be found in the selected modality.
- Keep each item single-purpose and evidence-based.
- Prefer concrete evidence over vague judgments.
- Do not invent case facts.
  Put uncertainty or acceptable alternatives in `AdditionalContext`.
- For audio rubrics, make each scoring level independently scorable from the audio
  recording or a reliable transcript.
- Grade only what can be heard or reliably transcribed.
  Avoid visual-only behaviors such as eye contact, posture, gestures, or physical-exam
  technique unless the learner explicitly verbalizes them in the audio.

* * *

*Rubric Maker YAML → MAPLES upload mapping v1.0 - Updated June 2026*
