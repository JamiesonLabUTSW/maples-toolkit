# Case Generation

Placeholder plugin for future UT REAL Project MAPLES OSCE case generation workflows.

This plugin is listed in the marketplace as `NOT_AVAILABLE` until it contains real skills, scripts, or other runtime surfaces.

## Python Requirements

This placeholder has no Python dependencies yet. When the plugin adds Python scripts, place its runtime dependencies in `plugins/case-generation/requirements.txt` and install them with:

```bash
python3 -m pip install -r plugins/case-generation/requirements.txt
```
