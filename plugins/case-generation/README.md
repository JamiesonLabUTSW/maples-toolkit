# Case Generation

Placeholder plugin for future UT REAL Project MAPLES OSCE case generation workflows.

This plugin is listed in the Codex marketplace as `NOT_AVAILABLE` until it contains real
skills, scripts, or other runtime surfaces.
It is intentionally omitted from the Claude Code marketplace until it becomes
installable.

## Python Requirements

This placeholder has no Python dependencies yet.
When the plugin adds Python scripts, place its runtime dependencies in
`plugins/case-generation/requirements.txt` and install them with:

```bash
python3 -m pip install -r plugins/case-generation/requirements.txt
```
